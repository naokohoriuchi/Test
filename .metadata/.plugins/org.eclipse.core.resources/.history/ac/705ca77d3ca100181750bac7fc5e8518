$(document).ready(function() {

	// 合計金額を計算して表示する
	var money = $(".mainMoney");
	var total = 0;
	var i = 0;

	$(".mainMoney").each(function() {
		Number(str); // 123

		total = total + $(this).text();
		//alert($(this).text());
	});

	$("#total").html(total + "円");
});